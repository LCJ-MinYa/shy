{
	"name": "init-node-project",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"build": "babel src -d lib",
		"start": "npm run build && node lib/index",
		"download": "npm run build && node lib/download"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"babel-cli": "^6.26.0",
		"babel-preset-es2015": "^6.24.1"
	},
	"dependencies": {
		"cheerio": "^1.0.0-rc.2",
		"iconv-lite": "^0.4.19",
		"mkdirp": "^0.5.1",
		"mongoose": "^5.0.2",
		"request": "^2.83.0"
	}
}
